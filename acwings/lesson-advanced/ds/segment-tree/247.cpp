#include <iostream>
#include <algorithm>
#include <cstring>

using namespace std;
const int N = 1e4 + 2;

int n;

int main() {
    int k = 0;
    while (true) {
        scanf("%d", &n);
        if (n == 0) break;
        k++;

        for (int i = 0; i < n; ++i) {

        }

        
    }
    

    return 0;
}